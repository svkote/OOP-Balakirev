type: choice
is_multiple_choice: true
options:
- text: перед началом выполнения операторов внутри менеджера контекста выполняется
    метод __exit__, а после завершения - метод __enter__
- text: менеджер контекста образует свою локальную область видимости (подобно функциям,
    тело которых находится внутри локальной области видимости)
- text: перед началом выполнения операторов внутри менеджера контекста выполняется
    метод __enter__, а после завершения - метод __exit__
- text: после оператора with необходимо указывать объект класса, в котором обязательно
    должен быть реализован метод __enter__, а метод __exit__ можно не прописывать
- text: после оператора with необходимо указывать объект класса, в котором обязательно
    должны быть реализованы методы __enter__ и __exit__
files:
- name: task.py
  visible: true
  text: |+
    #  You can experiment here, it won’t be checked

  learner_created: false
status: Unchecked
record: -1
local_check: false
